import React from "react";
import Header from "../../components/parts/Header";

const page = () => {
  const url = "/about";
  return (
    <div>
      <Header url={url} />
    </div>
  );
};

export default page;
